package com.smellyalater.corefunctionality.gui.chestgui.components

import org.bukkit.ChatColor
import org.bukkit.Material
import org.bukkit.entity.Player
import org.bukkit.inventory.ItemStack


class ShopGUI {

    companion object {

        fun Player.shopGUI() {
            /*
            var trades: ArrayList<VillagerTrade> = ArrayList()
            trades.add(
                VillagerTrade(
                ItemStack(Material.DIRT), ItemStack(Material.DIRT),
                ItemStack(Material.GLASS), 10)
            )

            var inventory = VillagerInventory(trades, this)
            inventory.name = ChatColor.translateAlternateColorCodes('&', "&aShop")
            inventory.open()
            */
        }
    }
}